<?xml version="1.0" encoding="utf-8"?>
<!-- Outgoing message bubble -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="4dp"
    android:gravity="end">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/message_container"
        android:orientation="vertical"
        android:layout_marginStart="60dp"
        android:background="@drawable/message_bubble_outgoing"
        android:padding="12dp"
        android:layout_gravity="end">

        <TextView
            android:id="@+id/message_body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="?attr/colorOnPrimaryContainer"
            android:lineSpacingExtra="2dp"
            android:autoLink="none" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_gravity="end"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/message_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="?attr/colorOnPrimaryContainer" />

            <FrameLayout
                android:id="@+id/message_sim_indicator"
                android:layout_width="18dp"
                android:layout_height="11dp"
                android:layout_marginStart="4dp"
                android:layout_gravity="center_vertical"
                android:visibility="gone">
                <ImageView
                    android:id="@+id/message_sim_icon"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:src="@drawable/ic_sim_horizontal"
                    app:tint="?attr/colorOnPrimaryContainer" />
                <TextView
                    android:id="@+id/message_sim_slot"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:textSize="7sp"
                    android:textStyle="bold" />
            </FrameLayout>

            <ImageView
                android:id="@+id/message_delivery_status"
                android:layout_width="11dp"
                android:layout_height="11dp"
                android:layout_marginStart="4dp"
                android:layout_gravity="center_vertical"
                android:src="@drawable/ic_tick_single"
                app:tint="?attr/colorOnPrimaryContainer"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
