<?xml version="1.0" encoding="utf-8"?>
<!-- Reusable message composer bar component -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/composer_bar_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/colorSurfaceVariant"
    android:paddingStart="12dp"
    android:paddingEnd="8dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <!-- Message input -->
    <EditText
        android:id="@+id/composer_message_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:minHeight="44dp"
        android:hint="Message"
        android:inputType="textCapSentences|textMultiLine"
        android:maxLines="4"
        android:background="@android:color/transparent"
        android:textSize="16sp"
        android:textColor="?android:attr/textColorPrimary"
        android:textColorHint="?attr/colorOnSurfaceVariant"
        android:paddingStart="4dp"
        android:paddingEnd="8dp" />

    <!-- Character counter -->
    <TextView
        android:id="@+id/composer_message_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:text="0"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="11sp" />

    <!-- SIM toggle (only visible for dual-SIM) -->
    <FrameLayout
        android:id="@+id/composer_sim_toggle"
        android:layout_width="28dp"
        android:layout_height="36dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone">

        <ImageView
            android:id="@+id/composer_sim_icon"
            android:layout_width="16dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_sim_card"
            app:tint="?attr/colorOnSurfaceVariant" />

        <TextView
            android:id="@+id/composer_sim_number"
            android:layout_width="16dp"
            android:layout_height="20dp"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingTop="4dp"
            android:text="1"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="9sp"
            android:textStyle="bold" />

    </FrameLayout>

    <!-- Send button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/composer_send_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:contentDescription="Send"
        android:src="@drawable/ic_send"
        app:elevation="0dp"
        app:fabSize="mini"
        app:fabCustomSize="40dp"
        app:maxImageSize="20dp"
        app:backgroundTint="?attr/colorPrimary"
        app:tint="?attr/colorOnPrimary" />

</LinearLayout>
