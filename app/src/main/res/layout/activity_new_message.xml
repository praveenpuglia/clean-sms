<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true">

    <!-- Page Header -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/colorBackground"
        android:elevation="3dp"
        android:fitsSystemWindows="false">

        <LinearLayout
            android:id="@+id/new_message_header_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fitsSystemWindows="false"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingTop="16dp"
            android:paddingEnd="20dp"
            android:paddingBottom="16dp">

            <LinearLayout
                android:id="@+id/new_message_header_title_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/new_message_back_button"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Back"
                    android:padding="8dp"
                    android:src="@drawable/ic_arrow_back"
                    app:tint="?attr/colorOnSurface" />

                <TextView
                    android:id="@+id/new_message_header_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:letterSpacing="0.015"
                    android:paddingStart="8dp"
                    android:text="New message"
                    android:textColor="?android:attr/textColorPrimary"
                    android:textSize="28sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_gravity="bottom"
            android:background="?attr/colorOutlineVariant" />
    </FrameLayout>

    <!-- Message input area (fills available space) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Message input that fills the space -->
        <EditText
            android:id="@+id/new_message_body_input"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:gravity="top|start"
            android:hint="Type your message..."
            android:inputType="textMultiLine|textCapSentences"
            android:padding="16dp"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textColor="?android:attr/textColorPrimary"
            android:textColorHint="?attr/colorOnSurfaceVariant" />

        <!-- Contact suggestions overlay -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/new_message_contacts_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/colorSurface"
            android:visibility="gone" />

    </FrameLayout>

    <!-- SMS counter info bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end|center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <!-- SMS parts indicator (shows when > 1 part) -->
        <TextView
            android:id="@+id/new_message_sms_parts"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="12sp"
            android:visibility="gone" />

        <!-- Character counter -->
        <TextView
            android:id="@+id/new_message_counter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="0 / 160"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="12sp" />

    </LinearLayout>

    <!-- Bottom bar: SIM selector + To field + Send button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="?attr/colorSurfaceVariant"
        android:elevation="4dp">

        <!-- Divider at top -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?attr/colorOutline" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:minHeight="56dp">

            <!-- To label -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="To:"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="14sp"
                android:paddingStart="4dp"
                android:paddingEnd="8dp" />

            <!-- Recipient chips and input container -->
            <HorizontalScrollView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:scrollbars="none"
                android:fillViewport="true">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <!-- ChipGroup for recipients -->
                    <com.google.android.material.chip.ChipGroup
                        android:id="@+id/new_message_recipients_chip_group"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        app:chipSpacingHorizontal="4dp"
                        app:singleLine="true"
                        app:selectionRequired="false" />

                    <!-- Inline recipient input -->
                    <EditText
                        android:id="@+id/new_message_recipient_input"
                        android:layout_width="100dp"
                        android:layout_height="40dp"
                        android:hint="Add recipient"
                        android:inputType="text|textNoSuggestions"
                        android:maxLines="1"
                        android:background="@android:color/transparent"
                        android:textSize="14sp"
                        android:textColor="?android:attr/textColorPrimary"
                        android:textColorHint="?attr/colorOnSurfaceVariant"
                        android:paddingStart="4dp"
                        android:paddingEnd="4dp"
                        android:imeOptions="actionDone" />

                </LinearLayout>
            </HorizontalScrollView>

            <!-- SIM toggle (only visible for dual-SIM) -->
            <FrameLayout
                android:id="@+id/new_message_sim_toggle"
                android:layout_width="32dp"
                android:layout_height="40dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/new_message_sim_icon"
                    android:layout_width="18dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_sim_card"
                    app:tint="?attr/colorOnSurfaceVariant" />

                <TextView
                    android:id="@+id/new_message_sim_number"
                    android:layout_width="18dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:paddingTop="5dp"
                    android:text="1"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:textSize="10sp"
                    android:textStyle="bold" />

            </FrameLayout>

            <!-- Send button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/new_message_send_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:contentDescription="Send"
                android:src="@drawable/ic_send"
                app:fabSize="mini"
                app:fabCustomSize="40dp"
                app:maxImageSize="20dp"
                app:elevation="0dp"
                app:backgroundTint="?attr/colorPrimary"
                app:tint="?attr/colorOnPrimary"
                android:enabled="false" />

        </LinearLayout>
    </LinearLayout>

</LinearLayout>
